[Service]
# --pipewire forces the Wayland/Portal grabber, --desktop keeps the systray.
# Empty ExecStart= first to clear the packaged one before replacing it.
ExecStart=
ExecStart=/bin/sh -c 'LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/lib/hyperhdr /usr/bin/hyperhdr --desktop --pipewire'
