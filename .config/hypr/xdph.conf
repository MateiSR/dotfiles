# Pre-tick "allow restoring" in the share picker. Without it a picker answered
# without the box yields no restore token, so the next portal session prompts
# again — HyperHDR's grabber bounces make that a loop.
screencopy {
    allow_token_by_default = true
}
